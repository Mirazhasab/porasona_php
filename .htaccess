<IfModule mod_rewrite.c>
    Options +FollowSymLinks
    RewriteEngine On

    # If the requested resource doesn't already point to /public, rewrite it
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
